$kind: http-request
description: Update details for an account.
url: "{{baseUrl}}/accounts/:accountNumber"
method: PATCH
headers:
  Content-Type: application/json
  Accept: application/json
pathVariables:
  - key: accountNumber
    value: "{{accountNumber}}"
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string"
          }
        },
        "required": [
          "value"
        ]
      }
body:
  type: json
  content: |-
    {
      "name": "New Business Account Name",
      "currency": "USD",
      "status": "active"
    }
  $type: |-
    {
      "type": "object",
      "properties": {
        "name": {
          "type": "string"
        },
        "currency": {
          "type": "string"
        },
        "status": {
          "type": "string"
        }
      }
    }
examples: ./.resources/Update Account.resources/examples
order: 3000
