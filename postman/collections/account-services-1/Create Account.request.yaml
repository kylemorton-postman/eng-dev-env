$kind: http-request
description: Create
url: "{{baseUrl}}/accounts/:accountNumber/create"
method: POST
headers:
  Content-Type: application/json
  Accept: application/json
pathVariables:
  accountNumber: "{{accountNumber}}"
body:
  type: json
  content: |-
    {
      "name": "string",
      "currency": "string"
    }
examples: .resources/Create Account.resources/examples
order: 3004895743443276
